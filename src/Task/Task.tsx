import React from 'react';

const Task = () => {
    return (
        <div className="taskBlock">
            <span className="taskSpan"></span>
            <button className="btnDelete">Delete</button>
        </div>
    );
};

export default Task;